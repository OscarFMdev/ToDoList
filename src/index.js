import './style.css';

const tasksContainer = document.querySelector('.tasks-container');

const tasks = [
  {
    index: 1,
    description: "Hello",
    completed: true
  },
  {
    index: 2,
    description: "World",
    completed: true
  },
  {
    index: 3,
    description: "!!!!",
    completed: true
  },
]

const createTasks = () => {
  tasks.forEach((task) => {
  });
}

createTasks();